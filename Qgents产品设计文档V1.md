# Qgents 产品设计文档（V1.0）

> 本文档用于说明 Qgents 的产品定位、核心流程和当前实现范围。
> 当前版本基于 Web `main`、Android `develop`、后端 `develop` 分支整理。

## 1. 产品概述

### 1.1 产品定位

Qgents 是一个面向团队软件开发的多人、多 Agent 协作平台。它以“项目群聊 + 任务执行 + 代码交付”为主线，将需求讨论、Agent 开发、测试审查、Diff 查看和 GitHub 交付放在同一个协作空间内。

它不是单纯的聊天机器人，而是一个以 Team 和 Project 为边界、以任务和代码变更为核心产物的协作开发系统。

### 1.2 解决的问题

- 需求讨论、开发过程和代码产物分散在不同工具中，过程难以追踪。
- 多个 Agent 之间缺少统一的任务上下文、代码工作区和交付流程。
- 团队成员无法清楚了解任务当前由谁执行、执行到哪一步、产生了哪些改动。
- 移动端通常只能接收消息，无法及时跟进任务、Diff 和合并请求。

### 1.3 产品目标

- 让团队在项目群聊中直接提出和跟进开发需求。
- 让 Agent 按计划执行开发、测试和审查，并保留可追踪记录。
- 让人能够在代码合入前查看 Diff、审查结果和 MR 状态。
- 让 Web 和 Android 端共享同一套项目、任务和消息状态。

## 2. 用户与角色

### 2.1 团队成员

- **Owner**：管理团队、项目和成员权限。
- **Member**：参与项目群聊、创建或跟进任务、查看代码交付结果。

具体权限由团队和项目边界控制，项目之间的数据相互隔离。

### 2.2 Agent 角色

- **Planner**：理解需求并拆分任务步骤。
- **Developer**：在受控工作区中修改代码。
- **Tester**：执行或组织验证，输出测试结果。
- **Reviewer**：审查代码变更和交付质量。

Agent 可关联 Skill 和 Memory，用于复用能力配置、历史上下文和项目知识。

## 3. 产品结构

### 3.1 Team 与 Project

Team 是成员和权限的组织边界，Project 是具体研发项目的工作边界。项目下集中管理：

- 项目群聊和需求群；
- Agent、Skill、Memory；
- GitHub 仓库、分支和工作区；
- Task、Task Step、Task Run；
- Diff、交付记录和 MR；
- Testset、Quality Gate 和 CQ Review。

### 3.2 项目群聊

群聊是需求进入开发流程的入口。成员可以在对话中描述需求、补充上下文，并触发任务执行。任务执行过程中的系统事件、Agent 消息和结果可以回到群聊或任务详情中查看。

### 3.3 任务中心

任务中心用于查看任务列表、执行状态和进度。一个 Task 可以包含多个 Task Step，每一步对应一次或多次 Task Run，从而记录计划、执行和结果。

任务详情重点展示：

- 任务描述和当前状态；
- Plan 拆分出的步骤；
- Agent 执行日志和结果；
- 测试、审查和质量门禁信息；
- 代码 Diff、交付状态和 MR。

### 3.4 代码交付

任务执行完成后，成员可以查看文件树、Diff 和交付中心信息。GitHub 集成支持项目仓库绑定、分支管理及 MR 相关信息查看。

代码交付以人工审查为重要节点，Agent 的输出需要经过 Diff 或 MR 层面的确认后再进入后续交付流程。

### 3.5 Android 移动端

Android 端面向移动场景，提供：

- 团队、项目和群聊访问；
- 需求和任务触发；
- 任务进度、执行日志和通知；
- Diff、Patch、交付和 MR 查看；
- 消息历史的本地缓存。

复杂配置、完整代码对比和管理操作主要由 Web 端承担，移动端重点保障及时跟进和审核查看。

## 4. 核心业务流程

```text
需求进入项目群聊
        |
        v
创建或触发 Task
        |
        v
Planner 拆分 Task Step
        |
        v
Developer 在 Workspace 中修改代码
        |
        v
Tester 执行验证 / 测试
        |
        v
Reviewer 进行代码和质量审查
        |
        v
生成 Diff / 交付记录 / MR 信息
        |
        v
成员确认并完成后续合入
```

当前实现中，测试失败并不一定立即终止整个任务，最终是否完成可能由后续 Review 结果和编排状态决定。因此产品界面需要同时展示“测试结果”和“任务最终状态”，不能只根据某一个步骤的结果判断任务是否完成。

## 5. 三端职责

| 端 | 主要职责 |
| --- | --- |
| Web | 完整工作台：项目管理、群聊、任务编排、Agent 配置、Skill/Memory、代码 Diff、Testset、质量审查和 MR 查看 |
| Android | 移动协作：消息、通知、任务跟进、执行日志、Diff/Patch/MR 查看 |
| 后端 | 统一权限和数据模型、任务编排、Agent 执行、Workspace/Sandbox、GitHub 集成、实时事件和交付状态 |

## 6. 当前已实现能力

- Team、Project、成员和项目隔离。
- 项目群聊、需求群聊和消息同步。
- Agent、Skill、Memory 管理。
- Task、Task Step、Task Run 及执行日志。
- Planner、Developer、Tester、Reviewer 的任务执行链路。
- GitHub OAuth / App 集成、项目仓库绑定和分支信息。
- Workspace、受控 Sandbox 和代码修改流程。
- 文件树、Diff Review、Delivery Center、Patch 和 MR 信息查看。
- Testset、Dry-run、Quality Gate、CQ Review 等质量相关能力。
- Web SSE、Android SSE/WebSocket，以及消息通知和移动端消息缓存。

## 7. 正在完善与后续规划

### 7.1 当前需要继续完善

- 在任务详情中明确展示 `executionMode`，区分开发、验证、测试和审查阶段。
- 明确“失败后阻断”“失败后继续审查”“返回修复”的产品规则。
- 让任务步骤按依赖关系执行，而不只是按步骤序号线性推进。
- 对人工测试且没有实际命令的结果，提供更明确的状态说明。
- 统一三端对任务阶段、失败原因和最终结论的展示语义。

### 7.2 后续规划

- P1：可靠消息、多端同步、分支/代码审查、Testset、Dry-run、CQ+1。
- P2：低代码工作流、Webhook/定时触发、Plugin 体系。
- 增强任务看板、执行过程可视化和失败后的修复闭环。
- 完善生产部署中的 HTTPS、密钥管理和 Sandbox 隔离策略。

## 8. 建议演示路径

### 8.1 从需求到交付

1. 创建 Team、Project、Agent，并绑定 GitHub 仓库。
2. 在项目群聊中提出开发需求。
3. 触发 Task，查看 Planner 拆分出的步骤。
4. 查看 Developer 在 Workspace/Sandbox 中的执行过程。
5. 查看测试结果、Reviewer 结论和任务状态。
6. 打开 Diff、Delivery Center 和 MR 信息，完成交付确认。

### 8.2 基于 Diff 继续追问

1. 打开任务的某个文件 Diff。
2. 围绕具体改动提出补充问题或修改要求。
3. Agent 基于已有任务上下文和当前代码状态继续修改。
4. 查看新的增量 Diff，并再次进行审查。

## 9. 版本说明

本文档为 V1.0 初稿，描述的是当前代码基线和已确认的产品方向，不等同于最终需求基线。后续接口、业务规则和三端交互发生变化时，应同步更新本文档。
